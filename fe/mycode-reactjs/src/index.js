import React from 'react';
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  // <h1>Học Hành Chăm Chỉ</h1>
  <HomePage/>
  <div>
    <h1>Học</h1>
  </div>
);